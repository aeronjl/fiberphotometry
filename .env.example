# Create a separate token at https://sandbox.zenodo.org/account/settings/applications/tokens/new/
# Never commit the real value. Loading this file is shell/tooling-specific.
ZENODO_SANDBOX_TOKEN=replace-with-a-sandbox-token
# Production drafts use a distinct token and require --production.
ZENODO_TOKEN=replace-with-a-production-token
